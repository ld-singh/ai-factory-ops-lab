# Module 02 — Slurm GPU Platform

> STATUS: PLANNED (Phase 3). Directory structure exists so the project map is
> complete; no content here claims to be implemented.

Will cover: Slurm-in-Docker cluster, fake GRES vs real `--gres=gpu` (strictly
separated), slurm.conf / gres.conf / cgroup.conf / slurmdbd.conf, GPU job
scripts (small, large, array, cuda-check), QoS limits, fair-share, accounting,
drain/resume drills, and pending-reason triage. Evidence will land in
`../06-validation-reports/slurm-gres-validation.md`.
