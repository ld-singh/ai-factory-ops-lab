# Module 04 — Inference Serving

> STATUS: PLANNED (Phase 5).

Will cover: Triton and/or vLLM serving a lightweight model, gateway/routing,
load tests, and benchmark reports (TTFT, p95/p99 latency, tokens/sec, req/sec,
GPU utilization/memory, error rate). Benchmark numbers will only ever be
published from real GPU runs; anything else is an unloaded template.
Also: inference vs training vs batch workload characteristics, and the
Kubernetes-vs-Slurm fit discussion for each.
